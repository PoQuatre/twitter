const { HashtagModel } = require('./hashtag.model');
const { LikeModel } = require('./like.model');
const { RetweetModel } = require('./retweet.model');
const { TweetModel } = require('./tweet.model');
const { UserModel } = require('./user.model');

module.exports = {
  HashtagModel,
  LikeModel,
  RetweetModel,
  TweetModel,
  UserModel,
};
